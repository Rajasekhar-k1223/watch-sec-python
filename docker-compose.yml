

services:
  backend:
    build: ./backend
    container_name: watch-sec-backend
    command: uvicorn app.main_dashboard:app --host 0.0.0.0 --port 8000
    ports:
      - "8000:8000"
    extra_hosts:
      - "host.docker.internal:host-gateway"
    env_file:
      - .env
    environment:
      - DATABASE_URL=mysql+aiomysql://appuser:${DB_PASSWORD}@watch-sec-sql:3306/appdb
      - MONGO_URL=mongodb://admin:${DB_PASSWORD}@watch-sec-mongo:27017/appdb?authSource=admin
      - REDIS_URL=redis://172.18.0.8:6379/0
      - CELERY_BROKER_URL=redis://172.18.0.8:6379/0
      - CELERY_RESULT_BACKEND=redis://172.18.0.8:6379/0
      - PYTHONUNBUFFERED=1
      - MONITORIX_BASE_URL=https://monitorix.co.in
    volumes:
      - ./backend/storage:/app/storage
      - ./backend:/app
    networks:
      - watch-sec-network

  agent-gateway:
    build: ./backend
    container_name: watch-sec-agent-gateway
    command: uvicorn app.main_agent:app --host 0.0.0.0 --port 8005
    ports:
      - "8005:8005"
    extra_hosts:
      - "host.docker.internal:host-gateway"
    env_file:
      - .env
    environment:
      - DATABASE_URL=mysql+aiomysql://appuser:${DB_PASSWORD}@watch-sec-sql:3306/appdb
      - MONGO_URL=mongodb://admin:${DB_PASSWORD}@watch-sec-mongo:27017/appdb?authSource=admin
      - REDIS_URL=redis://172.18.0.8:6379/0
      - CELERY_BROKER_URL=redis://172.18.0.8:6379/0
      - CELERY_RESULT_BACKEND=redis://172.18.0.8:6379/0
      - PYTHONUNBUFFERED=1
      - MONITORIX_BASE_URL=https://monitorix.co.in
    volumes:
      - ./backend/storage:/app/storage
      - ./backend:/app
    networks:
      - watch-sec-network

  frontend:
    build:
      context: ../watch-sec-frontend
      args:
        VITE_API_URL: /api
    container_name: watch-sec-frontend
    ports:
      - "8080:80"
    networks:
      - watch-sec-network

  celery:
    build: ./backend
    container_name: watch-sec-celery
    command: celery -A app.worker.celery_app worker --loglevel=info
    extra_hosts:
      - "host.docker.internal:host-gateway"
    env_file:
      - .env
    environment:
      - DATABASE_URL=mysql+aiomysql://appuser:${DB_PASSWORD}@watch-sec-sql:3306/appdb
      - MONGO_URL=mongodb://admin:${DB_PASSWORD}@watch-sec-mongo:27017/appdb?authSource=admin
      - REDIS_URL=redis://watch-sec-redis:6379/0
      - CELERY_BROKER_URL=redis://watch-sec-redis:6379/0
      - CELERY_RESULT_BACKEND=redis://watch-sec-redis:6379/0
      - MONITORIX_BASE_URL=https://monitorix.co.in
    volumes:
      - ./backend:/app
      - ./backend/storage:/app/storage
    networks:
      - watch-sec-network

networks:
  watch-sec-network:
    external: true
    name: watch-sec-network
